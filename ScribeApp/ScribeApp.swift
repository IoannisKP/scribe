import SwiftUI

@main
struct ScribeApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.automatic)
        .defaultSize(width: 960, height: 820)
    }
}
